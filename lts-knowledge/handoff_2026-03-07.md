# 引き継ぎドキュメント — 2026-03-07 セッション実施内容

> このドキュメントは、別のAIセッションに今回の作業内容を引き継ぐためのものです。

---

## 実施者
- 人間: 龍竹一生（kazui0907）
- AI: Claude Code（Opus 4.6 / Sonnet 4.6）

---

## 実施した作業の全体像

**目的:** 全社的なAI運用環境の標準化とモノレポの解体

大きく分けて3つのフェーズを実施しました。

---

## フェーズ1: Claude Code のセキュア環境構築

### 1-1. 危険設定の初期化
- `~/.claude/settings.json` から `skipDangerousModePermissionPrompt: true` を削除
  - ただし、Claude Code が再起動時にこの値を自動で書き戻す挙動あり（仕様上の制限）

### 1-2. プロジェクト信頼履歴のクリーンアップ
- `~/.claude.json` の `projects` から、`G:/` と `C:/` で始まるパスをすべて削除
- 残したのは `D:/scripts/ryouchiku-dashboard` と `D:/scripts/ryouchiku-systems` のみ
  - ※ ryouchiku-systems は今回廃止済みなので、次回セッションでこのエントリも消してよい

### 1-3. Gドライブ起動防止フック（SessionStart Hook）
- `~/.claude/settings.json` に `hooks.SessionStart` を追加
- スクリプト: `C:/Users/info/.claude/hooks/check-drive.js`
- 動作: `process.cwd()` に `G:` または `Google Drive` が含まれていたら警告を表示
- **制限事項:** SessionStart フックはセッションを強制終了させる機能を持たない（仕様）。警告は表示されるが、Claude Code は起動する

### 1-4. グローバル CLAUDE.md の作成
- `~/.claude/CLAUDE.md` を新規作成
- 内容: LTS全社共通AIルール（Plan Mode原則、フォルダ構成、本番反映ルール、lts-knowledge参照ルール）

---

## フェーズ2: lts-knowledge（全社ルールブック）の整備

### 対象ファイルと追記内容

| ファイル | 追記内容 |
|---------|---------|
| `02_folder_structure.md` | 各フォルダが独立Gitリポジトリである旨を明記。モノレポ解体後のリポジトリ一覧テーブルを追加 |
| `04_ai_development_flow.md` | Gドライブ安全装置の周知（冒頭に追加）。Step 5「本番反映」にn8n・GASの具体例を追記 |

---

## フェーズ3: モノレポ `ryouchiku-systems` の解体

### 背景
`ryouchiku-systems` は全事業のコードを1つのリポジトリに詰め込んだモノレポだった。
権限管理やAIのコンテキスト分離に支障があるため、プロジェクトごとの独立リポジトリに解体した。

### 移行マッピング

| 移行元（ryouchiku-systems内） | 移行先（D:/scripts/直下） | GitHubリポジトリ |
|------|------|------|
| `group/it-solution/sales-automation/` | `lts-sales-crm/` | `kazui0907/lts-sales-crm` |
| `group/it-solution/homepage-it/` | `it-homepage/` | `kazui0907/it-homepage` |
| `group/life-art/sales-auto/` | `lifeart-sales-auto/` | `kazui0907/lifeart-sales-auto` |
| `group/ryouchiku-dashboard/` | `ryouchiku-dashboard/` | `kazui0907/ryouchiku-dashboard` |
| `group/it-solution/gws-cli/` | `gws-cli/` | `kazui0907/gws-cli` |
| `scripts/philosophy-updater/` + `docs/` | `lts-internal/` | `kazui0907/lts-internal` |

### 空フォルダの処理
- `group/it-solution/clients/`（20クライアント分の .gitkeep のみ）→ 削除。必要時に `D:/scripts/{client-id}/` として新規作成する運用に変更
- `group/it-solution/lts-internal/`（.gitkeep のみ）→ 削除。`lts-internal` リポジトリに移行済み
- `group/nk-service/`, `group/life-art/`（.gitkeep のみ）→ 削除

### ryouchiku-systems の廃止
- GitHub上でアーカイブ済み（読み取り専用）
- ローカルフォルダは Windowsのファイルロックで削除できず残存
  - **次回やること:** `cd D:\scripts && rmdir /s /q ryouchiku-systems` で手動削除

### Git初期化時の問題
- Windows Defenderのリアルタイムスキャンが `.git/objects/` への書き込みをブロックする問題が発生
- 対処: `git add -A` を複数回リトライすることで全ファイルをステージング（5回以内に成功）
- 根本対策: 管理者権限でDefenderの除外パスに `D:\scripts` を追加すれば解消する

### GitHub認証
- `gh auth login` のブラウザ認証（モバイルデバイスコード）がうまくいかなかった
- Personal Access Token（Classic）で認証に成功
- 必要スコープ: `repo`, `workflow`, `read:org`, `admin:org`

---

## 現在の D:/scripts/ 構成

```
D:/scripts/
├── gws-cli/                 # Google Workspace CLI（Rust製）  → kazui0907/gws-cli
├── it-homepage/             # ITソリューション部HP           → kazui0907/it-homepage
├── lifeart-sales-auto/      # ライフアート営業自動化          → kazui0907/lifeart-sales-auto
├── lts-internal/            # 社内ツール（philosophy-updater）→ kazui0907/lts-internal
├── lts-knowledge/           # 全社ルールブック               → kazui0907/lts-knowledge
├── lts-sales-crm/           # 営業CRM（GAS）                → kazui0907/lts-sales-crm
├── ryouchiku-dashboard/     # 経営ダッシュボード（Next.js）   → kazui0907/ryouchiku-dashboard
└── ryouchiku-systems/       # 🗑️ 廃止済み（手動削除待ち）
```

---

## 現在のグローバル設定ファイル

| ファイル | 場所 | 内容 |
|---------|------|------|
| `settings.json` | `~/.claude/settings.json` | model: sonnet, SessionStart Hook |
| `CLAUDE.md` | `~/.claude/CLAUDE.md` | LTS全社共通AIルール |
| `check-drive.js` | `~/.claude/hooks/check-drive.js` | Gドライブ警告スクリプト |

---

## 今後やるべきこと（優先順）

### 必須
1. `ryouchiku-systems/` ローカルフォルダの手動削除（セッション終了後）
2. 各リポジトリに `CLAUDE.md` を作成（プロジェクト固有のAIルール）
3. `~/.claude.json` から `ryouchiku-systems` の信頼履歴エントリを削除

### 推奨
4. 各リポジトリのブランチを `master` → `main` に統一（`gh repo edit --default-branch main`）
5. Windows Defender の除外パスに `D:\scripts` を追加（管理者権限で実行）
6. `lts-knowledge` に `05_repository_guidelines.md`（各リポジトリのREADME/CLAUDE.mdテンプレート）を追加
7. GitHub Organization の作成を検討（`kazui0907` 個人アカウント → 組織アカウントへの移行）

### 検討事項
8. `gws-cli` は Google公式ツールのフォークなので、本当に自社リポジトリとして管理すべきか要検討
9. クライアント別リポジトリの作成タイミングと命名規則の運用ルール策定

---

*作成日: 2026-03-07*
